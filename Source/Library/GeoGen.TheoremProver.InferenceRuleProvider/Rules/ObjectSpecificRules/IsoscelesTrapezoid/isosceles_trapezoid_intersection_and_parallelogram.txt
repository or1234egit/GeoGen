# added
# reflection_in_isosceles_trapezoid_create_parallelogram
Objects:

 C = IsoscelesTrapezoidPoint(D, A, B)

Theorems:

Implications:

 () => EqualObjects: ReflectionInLineFromPoints(C, A, B), ParallelogramPoint(D, A, B)
